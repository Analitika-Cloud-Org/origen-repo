name: Mirror to Bitbucket
on:
  push:
    branches:
      - main
      - desa
      - cert
      - feature/*
      - hotfix/*
  workflow_dispatch:

jobs:
  mirror-to-bitbucket:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          # Solo configuramos el committer, mantenemos el autor original
          git config --global committer.name "Acloud DevOps"
          git config --global committer.email "devops@analitika.cloud"

      - name: Push to Bitbucket
        env:
          BITBUCKET_USERNAME: ${{ secrets.BITBUCKET_USERNAME }}
          BITBUCKET_APP_PASSWORD: ${{ secrets.BITBUCKET_APP_PASSWORD }}
          BITBUCKET_REPO: analitikacloud/destino-repo
          GIT_COMMITTER_NAME: "Acloud DevOps"
          GIT_COMMITTER_EMAIL: "devops@analitika.cloud"
        run: |
          echo "Configurando remote de Bitbucket..."
          git remote remove bitbucket || true
          git remote add bitbucket "https://$BITBUCKET_USERNAME:$BITBUCKET_APP_PASSWORD@bitbucket.org/$BITBUCKET_REPO.git"
          
          echo "Sincronizando ramas manteniendo autores originales..."
          # Push preservando autores originales
          git push bitbucket --all --force
          
          echo "Sincronizando tags..."
          git push bitbucket --tags --force
          
          if [ $? -eq 0 ]; then
            echo "✅ Sincronización completada exitosamente"
            echo "Los commits mantienen sus autores originales"
            echo "Committer configurado como: $GIT_COMMITTER_NAME"
          else
            echo "❌ Error en la sincronización"
            exit 1
          fi