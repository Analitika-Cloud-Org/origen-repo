name: Sync to Bitbucket

on:
  push:
    branches:
      - main
      - desa
      - cert
      - feature/*
      - hotfix/*
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Sync current branch to Bitbucket
        env:
          BITBUCKET_USERNAME: ${{ secrets.BITBUCKET_USERNAME }}
          BITBUCKET_APP_PASSWORD: ${{ secrets.BITBUCKET_APP_PASSWORD }}
        run: |
          # Obtener nombre de la rama actual
          CURRENT_BRANCH=${GITHUB_REF#refs/heads/}
          echo "📦 Sincronizando rama: $CURRENT_BRANCH"
          
          # Crear rama temporal desde el último commit
          echo "🔄 Creando rama temporal..."
          git checkout -b temp-sync-$CURRENT_BRANCH
          
          # Reescribir el historial de commits con el autor deseado
          git filter-branch -f --env-filter '
          export GIT_COMMITTER_NAME="ACloud DevOps"
          export GIT_COMMITTER_EMAIL="bot@analitika.cloud"
          export GIT_AUTHOR_NAME="ACloud DevOps"
          export GIT_AUTHOR_EMAIL="bot@analitika.cloud"
          ' HEAD
          
          # Eliminar workflows sin crear un nuevo commit
          echo "🗑️ Eliminando directorio workflows..."
          rm -rf .github/workflows/
          git update-index --skip-worktree .github/workflows/
          
          # Push a Bitbucket con el historial reescrito
          echo "⬆️ Realizando push a Bitbucket..."
          git remote add bitbucket https://$BITBUCKET_USERNAME:$BITBUCKET_APP_PASSWORD@bitbucket.org/analitikacloud/destino-repo.git
          git push bitbucket HEAD:$CURRENT_BRANCH --force
          
          # Limpiar
          echo "🧹 Limpiando..."
          git checkout $CURRENT_BRANCH
          git branch -D temp-sync-$CURRENT_BRANCH
          
          echo "✅ Sincronización completada exitosamente"